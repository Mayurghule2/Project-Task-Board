namespace TaskBoard.Api.Models
{
    public enum Priority
    {
        Low,
        Medium,
        High,
        Critical
    }

    public enum TaskStatus
    {
        Todo,
        InProgress,
        Review,
        Done
    }
}